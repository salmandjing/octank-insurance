# Octank Insurance Virtual Agent — Environment Variables
# Copy this to .env and fill in your values

# AWS Bedrock — uses credentials from ~/.aws/credentials or environment
AWS_REGION=us-east-1

# Models (two-tier for cost efficiency)
# Haiku for supervisor (classification) — cheap & fast
# Sonnet for specialists (reasoning + tool use) — better quality
SUPERVISOR_MODEL_ID=us.anthropic.claude-3-5-haiku-20241022-v1:0
SPECIALIST_MODEL_ID=us.anthropic.claude-3-5-sonnet-20241022-v2:0

# Agent settings
# MAX_AGENT_STEPS=5
# MAX_TOKENS=4096
# TEMPERATURE=0.1

# RAG settings
# RAG_CHUNK_SIZE=500
# RAG_CHUNK_OVERLAP=100
# RAG_TOP_K=4

# Session
# SESSION_TIMEOUT_MINUTES=30
